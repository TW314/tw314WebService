module.exports = atualizaInforacoesEmpresa => {
        const Empresa = app.db.models.empresa;
        Perfil.update(req.body, {
                where: {
                    id: req.params.id
                }
            }
        }
